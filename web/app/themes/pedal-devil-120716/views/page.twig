{% extends "base.twig" %}
{% block content %}
	{% include 'layout/acf/breadcrumb-nav.twig' with {'padding' : '90px'} %}

	<div class="l-page-section">
		<div class="container">
			<div class="row">
				<div class="col-md-8 col-md-offset-2">
					<h1 class="text-center">{{post.title}}</h1>
					{{post.content}}
				</div>
			</div>
		</div>
	</div>

	{% include 'footer.twig' with {'pullup': '120'} %}
{% endblock %}

